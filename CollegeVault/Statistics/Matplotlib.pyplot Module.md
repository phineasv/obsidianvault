```python
import matplotlib.pyplot as plt

plt.clf() # Chart not stacked up and mixed with each other

plt.figure() # Create figure

plt.xticks(rotation = 30; fontsize = 10) # Adjust xticks (The x items)

plt.xlabel(columnname, fontsize = 10) # Adjust x label
```